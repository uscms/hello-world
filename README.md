# Hello World

一个由 WorkBuddy 生成并自动部署到 GitHub Pages 的极简静态网页示例。

- 纯 HTML / CSS / JS，无任何外部依赖
- 响应式设计，支持移动端
- 通过 GitHub Actions 自动部署到 `gh-pages` 分支

## 本地预览

直接用浏览器打开 `index.html` 即可。

## 在线访问

部署完成后访问：`https://<username>.github.io/hello-world/`

## 重新部署

推送代码到 `main` 分支会自动触发 GitHub Actions 工作流完成部署。
